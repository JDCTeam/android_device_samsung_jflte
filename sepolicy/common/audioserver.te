allow audioserver qmuxd_socket:dir w_dir_perms;
allow audioserver qmuxd_socket:sock_file create_file_perms;
allow audioserver self:socket create_socket_perms;

unix_socket_connect(audioserver, qmuxd, qmuxd)
